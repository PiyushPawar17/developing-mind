import React from 'react';
import { Link } from 'gatsby';

import '../styles/Navbar.css';

const Navbar = () => (
	<nav className="navbar">
		<Link to="/">DevelopingMind</Link>
	</nav>
);

export default Navbar;
